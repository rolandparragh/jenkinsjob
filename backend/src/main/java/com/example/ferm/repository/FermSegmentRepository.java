package com.example.ferm.repository;

import com.example.ferm.domain.FermSegment;
import org.springframework.data.jpa.repository.JpaRepository;

public interface FermSegmentRepository extends JpaRepository<FermSegment, Long> {
}
